import { cardOnly } from "../../components/cards/cardOnly/cardOnly.js";

const containerDescription = document.querySelector("container-group");

containerDescription.appendChild(cardOnly("Grupo dinamita", "Leisy"));